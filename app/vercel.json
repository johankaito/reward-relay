{
  "git": {
    "deploymentEnabled": false
  },
  "buildCommand": "rm -rf node_modules .next && pnpm install --force && pnpm run build",
  "installCommand": "rm -rf node_modules && pnpm install --force"
}
