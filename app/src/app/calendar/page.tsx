// ARCHIVED: Card application calendar view — experimental feature, not part of the MVP nav.
// Full implementation (377 lines) preserved in git history (pre chore/route-consolidation).
// Redirects to /dashboard until a calendar feature is formally scoped.
import { redirect } from 'next/navigation'
export default function CalendarPage() { redirect('/dashboard') }
