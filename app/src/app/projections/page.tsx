// MERGED: Functionality consolidated into /flights.
// Unique content integrated: "Top Cards to Close the Gap" card recommendations,
// monthly earning rate calculation, and months-to-goal estimate.
// /flights is the canonical award-flight route (Stitch design spec).
import { redirect } from 'next/navigation'
export default function ProjectionsPage() { redirect('/flights') }
