// ARCHIVED: Credit Inquiry Tracker — part of the Credit Inquiry Tracker backlog epic (CI-3/CI-4).
// Not part of the user-facing MVP. Full implementation (569 lines) preserved in git history.
// Redirects to /dashboard until the Credit Inquiry feature ships.
import { redirect } from 'next/navigation'
export default function InquiriesPage() { redirect('/dashboard') }
