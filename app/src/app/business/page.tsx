// ARCHIVED: Business tier FBT/tax record keeper — part of the Business Tier backlog epic (BT-4/BT-5).
// Not part of the user-facing MVP. Full implementation preserved in git history.
// Redirects to /dashboard until the Business Tier feature ships.
import { redirect } from 'next/navigation'
export default function BusinessPage() { redirect('/dashboard') }
