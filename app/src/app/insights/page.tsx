'use client'

// /insights is the public-facing route for the Net Profit dashboard (NP epic).
// The full implementation lives at /profit — this page re-exports it.
export { default } from '@/app/profit/page'
